Build with `npm run build`, build automatically when coding with `npm run watch`. Test CLI locally with `npx ts-node dist/cli.js {your commands here}` or debug in VSCode, which will prompt for the CLI arguments.
